---
date: 2026-04-13
status: completed
related_specs: [specs/模块-parser/README.md, specs/]
---

> **V1 落地情况（2026-04-15）**
> - GoogleTest 基础设施已引入（之前的 Frontmatter / ThemeLoader / SyntaxHighlighter / TocPanel 已在跑）
> - 本轮新增 `tests/parser/MarkdownParserTest.cpp`：12 个用例覆盖标题 / 列表 / 代码块 / 行内代码 / 链接 / 图片 / 引用 / 表格 / 换行 / 强调 / 空输入 / 行号契约
> - CI 中 ctest 已集成；ctest 5/5 全绿
> - 后续继续覆盖：PreviewLayout / SyntaxHighlighter 更细粒度场景（本 plan 不再承接，由专门 plan 跟进）

# 核心模块单元测试覆盖

## 背景
SDD 转型后所有 Spec 都有 T 编号，但代码侧测试用例尚未系统化。Parser / Layout / SyntaxHighlighter 等核心模块缺少自动化回归测试，重构时易引入回归。

## 动作
- [ ] 引入 GoogleTest（CMake `enable_testing()` + `gtest`）
- [ ] 优先覆盖：
  - [ ] Parser：标题/列表/代码块/表格/链接/图片
  - [ ] PreviewLayout：高 DPI 度量、嵌套坐标、行高一致性（对应 INV-DPI-* / INV-COORD-*）
  - [ ] SyntaxHighlighter：Markdown 关键 token
- [ ] 测试名嵌入 Spec T 编号：`TEST(PreviewLayoutTest, T1_HighDpiNoExtraSpace)`
- [ ] CI 中加入 `ctest --output-on-failure`
- [ ] 覆盖率目标：核心模块 ≥ 60%

## 验收
- `cd build && ctest -C Release --output-on-failure` 全绿
- CI 流程（Windows + Linux）跑通测试步骤
- 覆盖率报告显示核心模块达标
